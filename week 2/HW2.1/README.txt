CCE Backwards hat anweisung der inputs falsch (glaubt jonas) mögliche extrapoints...

 (which essentially !!!MEANS !!! compute the respective
Jacobian matrices for optimizing the network).
Since Backpropagation happens from back to front, we will also (((work through
follow this idea here:)))!?!?
